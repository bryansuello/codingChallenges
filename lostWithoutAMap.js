// Given an array of integers, return a new array with each value doubled.

// For example:

// [1, 2, 3] --> [2, 4, 6]

let arr = [1, 2, 3];

function maps(x) {
  return x.map(m => m * 2);
}

console.log(maps(arr));
